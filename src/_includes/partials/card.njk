{% macro card(post) %}
<a href="{{ post.url }}">
    <header>
        {% if post.feature_image %}
        <img src="{{ post.feature_image }}" alt="{{ post.title }}">
        {% endif %}
        <div>
            {% if post.tags %}{% for tag in post.tags %}{{ tag.name }} {% endfor %}{% endif %}
        </div>
        <h2>
            {{ post.title }}
        </h2>
    </header>
    <div>
        <p>{{ post.excerpt }}</p>
    </div>
    <footer>
        <div>
            <img src="{{ post.primary_author.profile_image }}" alt="{{ post.primary_author.name }}" />
        </div>
        <span>{{ post.primary_author.name }}</span>
        <div>
            {{ post.html | safe | getReadingTime }} min read
        </div>
    </footer>
</a>
{% endmacro %}